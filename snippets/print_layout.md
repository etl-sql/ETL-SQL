---
prefix: PRINT_LAYOUT
description: "Define print layout overrides for an individual visual (e.g. keep-together, page breaks, or exclusion)."
---
PRINT_LAYOUT = (
    PAGE_BREAK_BEFORE = ON,
    KEEP_TOGETHER = ON,
    EXCLUDE_FROM_PRINT = OFF
)
