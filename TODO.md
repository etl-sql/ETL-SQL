# ETL-SQL Development TODO List

## Bugs
### Security / Zero-Trust
- [x] Split file prefix can escape the validated destination directory → Fixed: `SplitFileStatementHandler` now rejects empty/rooted/path-traversal prefixes, validates each generated part path with `ValidatePath`/`ValidateWriteAccess`/`ValidateFileType`, and verifies every part remains under the resolved destination directory. Added `TestSplitFileRejectsUnsafePrefix`.
- [x] LDAP portal auth still contains diagnostic console/file logging that leaks directory details and bypasses configured logging → Fixed: removed `[LDAP-DIAG]` console writes and all hard-coded `ldap_debug.txt` writes; LDAP diagnostics now use structured `ILogger` with sanitized message text.
- [x] SharePoint connector prints request URLs and may expose site/library/file paths in logs → Fixed: removed `System.Console.WriteLine("[TEST-PRINT] ...")` calls from SharePoint file listing/download paths and removed the mock handler print in connector tests.
- [x] Portal refresh tokens remain valid after a user is disabled → Fixed: `AuthController.Refresh` now rejects and revokes refresh tokens for inactive users, and admin deactivation revokes outstanding refresh tokens. Extended the deactivated-user portal regression test to cover refresh.
- [x] Portal Structure modal: dark minimap + Soloed/Focused badges not hiding on click → Fixed (CSS-only, `designer.css`): (1) `.etlsql-dag-minimap` background was hard-coded `rgba(15,23,42,0.9)` — now `var(--portal-surface, #ffffff)` to match the light theme (canvas dots use `_nodeColor`, viewport rect is blue `rgba(37,99,235)` — both visible on light). (2) `.etlsql-dag-focusbadge { display: flex !important }` overrode the component's inline `style.display = 'none'`, so the badge could never hide; removed `!important` from that one property so the JS toggle works again (solo badge inherits it). Synced to host copies. Visual result not re-verified in a browser here — confirm in the portal Structure modal.

### Connectors
- [x] SharePoint connector documentation says `PASSWORD`/`CLIENT_SECRET` can use `ENC:`, but the implementation does not decrypt them → Fixed: SharePoint `AD_WINDOWS` password and Entra `CLIENT_SECRET` now decrypt `ENC:` values through the execution context, and SharePoint provider response bodies are no longer reflected in credential-sensitive errors.
- [x] New connector option aliases violate the no-alias connector standard → Fixed: removed S3 `ACCESSKEY`/`SECRETKEY`, Active Directory `LDAPS`, Kafka `SERVERS`, and MongoDB `URI`/`SERVER`/`UID`/`DB` option fallbacks; updated help/docs/snippets and added metadata assertions.

### File Operations
- [x] `MERGE FILES` can destroy an existing destination when the source pattern matches nothing → Fixed: source files are now resolved before destination replacement; zero matches throw and leave the destination unchanged. Added `TestMergeFilesNoMatchesPreservesDestination`.
- [x] `SYNC DIRECTORY DELETE_EXTRA` bypasses the file-operation guardrail spirit and under-counts destructive work → Fixed: `DELETE_EXTRA` now validates file types, increments file-operation counts for each file/directory delete, and checks recursive depth before recursive sync traversal.

### Engine / SQL Semantics
- [x] `LEFT SEMI JOIN` / `LEFT ANTI JOIN` were executed as left outer joins → Fixed: `JoinEngine` now treats prefixed and bare semi/anti join forms with the same semi/anti semantics instead of letting `LEFT` route them through outer-join handling. Added `semi_anti_join_reconciliation` scenario coverage.
- [x] Recursive CTE numeric columns widen to decimal-style values when materialized → Fixed: recursive CTE schema inference now treats integral decimal values as `INT`, preserving integer-shaped anchor/source columns through recursive materialization. Tightened `recursive_cte_hierarchy_rollup` back to integer-shaped expectations.

### Report Portal
- [x] Structure DAG jumbled on large maps → Fixed: `render()` in `designer.js` now computes a fit-to-view zoom from the actual node bounding box (`min(containerW/dataW, containerH/dataH, 1.0)`, floor 0.15) so all nodes are visible on first open regardless of graph size. `roam: true` still lets users pan/zoom further.
- [x] Dependencies transformation column shows only category name → Fixed: `formatTransformationKind(kind, expr, fns)` now shows the actual function names for Aggregation/FunctionCall (e.g. "SUM", "COUNT", "UPPER()"), extracts the target type for Cast (e.g. "→ INT"), and keeps the full expression as a hover tooltip.
- [x] Structure DAG emits no page→visual edges when visuals are declared before pages → Fixed: `GetStructure` now derives visual membership from each `CREATE PAGE` `MAP`/slot layout instead of statement order, records all pages on the visual metadata, and emits page→visual edges even when visuals are declared first. Added `Structure_UsesPageLayoutForVisualEdges_WhenVisualsDeclaredBeforePages`.
- [x] Report designer generates invalid dataset syntax → Fixed: `DesignerController.StateToScript` now emits `CREATE DATASET &name` and `SOURCE = &name` for report datasets. Added `DesignerControllerTests.Generate_UsesReportDatasetIdentifiers`.

### Lineage
- [x] `CREATE DATASET` column lineage was keyed to the generic `RESULTSET` target → Fixed: `LineageAnalyzer` (via `_selectTargetOverride`) and runtime `LineageManager.RecordCreateDatasetLineage` now record the inner SELECT's column lineage against `dataset:{name}`, carrying source columns + inherited descriptions/tags, so dataset column lineage persists and resolves by name across scripts. Tests: `LineageAnalyzerTests.Analyze_CreateDataset_KeysColumnLineageToDatasetName`, `EvenMoreHandlerTests.CreateDataset_RecordsReportObjectLineage`.
- [x] `LineageHistory` persistence dropped column-level fidelity → Fixed: store now persists + reads back `SourceColumns`, `TransformationKind`, `TransformationExpression`, `FunctionsApplied`, `DerivedFromDescriptions` (schema + ALTER migration + `SaveLineageAsync` + all read queries + `LineageHistoryEntry`). Test: `LineageCatalogTests.SaveAndQuery_PersistsColumnTransformAndDescription`.
- [x] Structure detail panel couldn't cross script boundaries to a separately-built dataset → Fixed: `GetStructure` `BridgeDatasetLineageAsync` resolves dataset references by stitching the registered `SourceQuery` parse (transform + source columns) with the persisted lineage catalog (description/tags), and propagates pass-through columns to `SELECT *` consumers. Verified end-to-end against the real portal endpoint + stores by `PortalIntegrationTests.Structure_BridgesDatasetReferenceAcrossScripts` (register dataset + persist build lineage → `GET /structure` resolves `total = SUM(Amount) ← edw.Sales.Amount` with description + pii across the boundary). Also fixed: a bare `SELECT *` consumer was recording a junk `"*"` column that blocked the bridge — `GetStructure` now skips `"*"` columns so the dataset's real columns propagate.
- [x] Descriptions required manual ETL-SQL tagging, not a live source-DB column comment → Fixed (opt-in): when `Lineage:ImportCatalogMetadata` is on (appsettings.json) or `SET LINEAGE_IMPORT_CATALOG = ON`, the engine lazily imports a DB source table's catalog metadata the first time it's read (`Evaluator.EnsureCatalogMetadataImportedAsync`, called from the `SELECT INTO` / `CREATE DATASET` handlers before lineage is recorded). A column's native comment is recorded as the lineage description (`d`) — not a `db_description` tag — so it inherits onto derived columns and surfaces in the structure panel. SQL Server/PostgreSQL/MySQL catalog providers already read comments. **Off by default** to avoid catalog-query latency; documented prominently in the Administrator's Guide and User Manual. Verified: `LineageAnalyzerTests.Analyze_InheritsDbColumnDescriptionOntoDerivedColumn` (comment → `d` → inherits onto a derived column with pii — this test pre-seeds the tracker, proving the d→inherit contract), `CatalogMetadataImportTests.Evaluator_LazilyImportsCatalogMetadataBeforeSelectLineage` (evaluator hook fires during `SELECT INTO`, records `DB_CATALOG`, and propagates the description/tags), and Docker-backed provider checks in `SqlServerTests`, `PostgresTests`, and `MySqlTests` (real `sys.extended_properties`, `col_description`, and `COLUMN_COMMENT` reads). Oracle/Snowflake/BigQuery catalog providers can be added later to extend coverage.
- [x] Surface the new persisted lineage fields in the catalog Lineage view → Fixed: `CatalogLineageHistoryDto` + `CatalogController.ToLineageDtosAsync` now expose `SourceColumns`, `TransformationKind`, `TransformationExpression`, `FunctionsApplied`, `DerivedFromDescriptions`; the portal Lineage view (`renderLineageRow`) shows a transform badge, source columns, and the inherited description, and the CSV export includes Transform + Description. Verified by `PortalIntegrationTests.CatalogLineageEndpoints_ReturnHistoryWithReportContext` (rich fields round-trip persist → store → DTO → JSON).

### Lineage — verification gaps (from the lineage-work audit)
- [x] Dockerized integration test for the live source-DB column-comment read → existing Testcontainers-backed `SqlServerTests`, `PostgresTests`, and `MySqlTests` now create a commented `amount` column and assert the real catalog provider reads the native description from `sys.extended_properties`, `pg_catalog.col_description`, and `information_schema.columns.COLUMN_COMMENT`.
- [x] Test the evaluator lazy catalog-import hook end-to-end → covered by `CatalogMetadataImportTests.Evaluator_LazilyImportsCatalogMetadataBeforeSelectLineage` and `CatalogMetadataImportTests.Evaluator_DoesNotImportCatalogMetadataWhenDisabled`: a catalog-backed mock data source runs through the evaluator, `SELECT INTO` imports `DB_CATALOG` metadata before handler lineage, inherited descriptions/tags propagate to the derived output column, and the provider remains untouched when catalog import is off.
- [x] Automated frontend smoke for the Structure detail lineage chain → `scripts/test-lineage-ui.mjs` imports canonical `designer.js` with a no-dependency DOM/ECharts shim, opens the visual and table detail panels through `renderDag().showDetail(...)`, and asserts field mapping, pass-through hops, transform expression, inherited description, and tags render. Wired into `test-lane` `fast`/`portal`/`full`.
- [x] Frontend smoke for the portal catalog lineage UI → extracted the lineage/dependency renderers from `src/ETL-SQL.ReportPortal/wwwroot/index.html` into `src/ETL-SQL.ReportPortal/wwwroot/js/lineage-ui.js`, fixed the missing `formatTags` helper used by the Lineage view, added a `tools/ui-sandbox` story for catalog result rows, dependency body, and CSV output, and expanded `scripts/test-lineage-ui.mjs` to assert rendered row fields, dependency lineage tags/descriptions, and CSV Transform/Description/Tags fields.
- [x] `SELECT *` from a raw DB table (not a dataset) showed no columns in the structure panel → Fixed without portal-time DB round-trips: `GetStructure` now enriches raw table nodes from persisted `DB_CATALOG` lineage when available, then pass-throughs those columns to single-source `SELECT *` consumers. Verified by `PortalIntegrationTests.Structure_ExpandsRawSelectStarFromPersistedCatalogLineage`.

## Tooling / Dev Experience
- Generalize the no-Docker preview harness into a component sandbox (`tools/ui-sandbox/`, renamed from `tools/dag-preview/`). Imports the canonical source directly (no sync / portal build / Docker); `pwsh -File tools\ui-sandbox\serve.ps1`. Storybook-style: sidebar of "stories" (one component) + a fixture picker.
  - [x] Phase 1 — stories framework + the three `designer.js` exports: `renderDag` (graph fixtures), `createScriptEditor` (CodeMirror), `createDesigner` (seeded `designState` + an injected mock fetch in `mockApi.js` for `/api/designer/parse|generate`, `onSaveScript` bypasses save). Shell `sandbox.js`, `util.js`, `stories/*.story.js`. Committed `0b886c63`; live-verified in-browser that the DAG story mounts. (Editor/designer stories built but not yet eyeballed live.)
  - [x] Phase 2 — lineage/dependencies modal renderers as stories. Extracted the shared render functions into `src/ETL-SQL.ReportPortal/wwwroot/js/lineage-ui.js`; `tools/ui-sandbox/stories/lineage-ui.story.js` hosts catalog result row, dependency body, and CSV fixtures without Docker or portal startup.
  - [x] Phase 3 — VS Code webview components hosted with their `vscode` API shimmed. `tools/ui-sandbox/stories/vscode-webviews.story.js` now hosts Results panel (built React UI + mocked protocol messages), Report Preview (shared runtime + sample manifest), and Report Designer (VS Code-style iframe wrapper + mock parse/generate/save bridge).

## Testing Foundation
- [x] Add a compact lane inventory report script → `scripts\Get-TestLaneInventory.ps1` outputs Markdown/JSON project/test counts by lane (`smoke`, `fast`, `engine`, `portal`, `integration`, `perf`, `slt`, `full`) so coverage organization is visible without reading filters by hand.
- [x] Bring `tests\ETL-SQL.Tests\Hardening\Performance` into the perf lane → classes are tagged `Category=Performance`, `scripts\test-lane.ps1`, `scripts\test-lane.sh`, and `scripts\Get-TestLaneInventory.ps1` include engine performance tests, and `.\scripts\test-lane.ps1 -Lane perf -NoRestore` passes.
- [x] Audit remaining tests under `tests\ETL-SQL.Tests\Integration` → true external-boundary tests are selected by `Category=Integration`; local correctness/UI/connector/engine/orchestration/security regressions have been moved into fast-covered folders. Current static inventory gap: 0 test methods excluded from `fast`/`engine` by name but not selected by a targeted lane.
- [x] Keep `Test-PreRelease.ps1 -Explain`, `Docs/Testing.md`, `Docs/Strategy/Test_Strategy.md`, and `scripts/README.md` synchronized whenever phases or lanes change → checked `-Explain -IncludeSlt -IncludeDockerIntegration -IncludeStandardScale -BuildInstallers -Platforms win-x64` and documented the current full phase plan.
- [x] Add new ETL scenario golden tests only for uncovered release claims; current scenario suite has 27 passing workflows covering staged ETL, `WHAT_IF`, lineage/tags/source columns, loops, `TRY...CATCH`, transactions, DML audit, merge, hash-change detection, set ops, recursive CTE, pivot/unpivot, semi/anti joins, file round trip, JSON extraction, and modular scripts. Audited against `Docs/Strategy/Release_Capability_Matrix.md` and found no uncovered ETL orchestration release claims; remaining Yellow items are explicit release gates (SLT, Docker integration, scale, installers), not scenario gaps.
- [x] Expand custom SLT files only when SQL semantics change or `Docs/Standards/SLT_Coverage.md` marks a release-relevant area Low/Medium and under-tested → expanded `match_recognize.test` from one happy-path query to one-row measures, `ALL ROWS PER MATCH` classifier output, and repeated match numbering; fixed the `REPLACE INTO` evidence fixture so the SLT lane validates replacement semantics instead of failing on unrelated string-width validation.

## Documentation Gaps (from gap analysis)
- [x] Roadmap language in reference docs – remove/replace occurrences of “roadmap”, “backlog”, “planned”, “future”.
- [x] Strategy folder status flags – add **Status:** header to each strategy file.
- [x] DocSanity test coverage – extend test or update docs to remove “backlog”, “planned”.
- [x] Standard Library missing function signatures – add signatures, return types, examples for BITCOUNT, BIT_SHIFTLEFT, BIT_SHIFTRIGHT.
- [x] Connector option naming consistency – rename `Password` option to `PASSWORD` in Data_Connectors.md.
- [x] CI workflow badge – replace Azure DevOps badge with GitHub Actions badge.
- [x] Add unit test for BIT_COUNT function in Tests.
- [x] Add license header to generated assets under src/ETL‑SQL.ReportRuntime/Resources/Shared.

### Architecture Documentation Gaps (June 2026 Audit)
- [x] [Connectors.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Connectors.md): Add the 8 missing connectors (SQLite, MySQL, MongoDB, Kafka, SharePoint, S3, ActiveDirectory, Orchestrator) to the inventory table.
- [x] [Connectors.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Connectors.md): Document `IPortalAdminConnection` and `ISpillable` on `InMemoryDataSource`.
- [x] [Connectors.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Connectors.md) & [Lineage.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Lineage.md): Verified C# namespaces (they are correct as `ETL_SQL.Data`, not `ETL_SQL.Core.Data`).
- [x] [Connectors.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Connectors.md) & [Engine.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Engine.md): Update `IConnector` and `IDataSource` signature listings to match C# source (include `IExecutionContext` parameters and missing default methods like `GetConfig()`).
- [x] [Lineage.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Lineage.md): Align `TransformationKind` enum values with [TransformationKind.cs](file:///C:/Users/chuck/scratch/ETL-SQL/src/ETL-SQL.Core/TransformationKind.cs) (update from 5 to 12 values).
- [x] [Engine.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/Engine.md): Update lint rules documentation to reflect the current 39 rules (currently docs show 18 rules).
- [x] [ExpressionEvaluation.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/ExpressionEvaluation.md): Re-order sections 14 and 15, and resolve internal NULL comparison contradictions (§6 vs §15).
- [x] [ParserLexer.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/ParserLexer.md): Correct description about `StatementParser.cs` being split into partials (it is monolithic).
- [x] [VariableScoping.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/VariableScoping.md): Clarify `Fork()` deep vs. shallow copy semantics.
- [x] [PortalUI.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/PortalUI.md): Replace references to `sync-assets.ps1` with `sync-assets.js` to match AGENTS.md.
- [x] [VSCodeExtension.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/VSCodeExtension.md): Document missing webview and command controllers (e.g. `reportDesignerPanel.ts`, `sidebarProvider.ts`, `WelcomeView.ts`, notebook support).
- [x] [TuiEditor.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/TuiEditor.md): Document 15 missing UI classes including `ReplUi.cs` and TUI report preview support.
- [x] [LanguageServer.md](file:///C:/Users/chuck/scratch/ETL-SQL/Docs/Architecture/LanguageServer.md): Document missing LSP handlers (`DesignerLspHandler.cs`, `DocumentSymbolProvider.cs`, `UpdateNotebookContextHandler.cs`).
- [x] Cross-references: Connect/cross-reference Reporting, Portal, UI, and Orchestrator architecture files to eliminate documentation siloing.

## VS Code Extension Audit (June 2026 Fresh Eyes Review)
### Single Responsibility Principle (SRP)
- [ ] **extension.ts bloat**: Refactor `extension.ts` to separate concern groups. It spans over 1,000 lines handling command registration, workspace events, process warmth, configuration checks, and Unix permission adjustments. These should be split into modules (e.g. `permissions.ts`, `terminalCommandBuilder.ts`, `cleanupService.ts`).
- [ ] **WelcomeView path resolution coupling**: Decouple `WelcomeView.ts` from direct knowledge of local/online files. The path resolver logic should be moved into a shared helper module.

### Logging
- [ ] **Webview logger interfaces**: Webviews (e.g. `ResultsPanel`, `ReportPreviewPanel`) write directly to browser console (`console.error`, `console.warn`). They should post messages back to the extension host to write to the unified `ETL-SQL` output channel for consolidated developer diagnostics.
- [ ] **Silent warmup failures**: Warmup process failures in `extension.ts` (`warmupRepl`) are caught and silenced. While intentional for happy-path user experience, recording warning telemetry in the output channel would greatly simplify troubleshooting of environment-related launch issues.

### Security
- [ ] **Cryptographic nonces in Webviews**: `resultsPanel.ts` and `sidebarProvider.ts` use `Math.random()` to generate nonces. While not highly sensitive, they should align with the standard in `reportPreviewPanel.ts` and `reportDesignerPanel.ts` which use `crypto.randomBytes(16).toString('base64url')` to prevent potential predictable-generator collisions.
- [ ] **Unprotected globalState store**: Storing connections in globalState (`etlsql.connections`) is currently unused but left in code. If global connection storage is reintroduced, it must use the VS Code `SecretStorage` API to protect credential values rather than simple global state JSON strings.

### Performance
- [ ] **Webview HTML loading cache**: Both `resultsPanel.ts` and `sidebarProvider.ts` synchronously read `index.html` from disk (`fs.readFileSync(...)`) on every webview resolution. Caching this string in memory after the first read will improve panel loading and UI render responsiveness.
- [ ] **Warmup concurrency lock**: Warmup and execute requests do not share a state lock. If a user quickly presses execute while warmup is starting, it may result in duplicate process spawn attempts.

### Linting
- [ ] **Clean remaining ESLint warnings**: 9 warnings exist in the workspace (inactive variable/exception parameters in `connectionsProvider.ts`, `sidebarProvider.ts`, and test files). These should be fixed to maintain a strictly zero-warning lint build.

