# ETL-SQL.ReportHosting

`ETL-SQL.ReportHosting` owns reusable report-session services shared by host shells.

Use this project for behavior that needs Engine execution plus Reporting manifest construction and is shared by ReportPlayer, ReportPortal, or other hosts. Keep HTTP routing, authentication, authorization, static files, portal persistence, and UX-specific behavior in the host projects.
