# LAST_VALUE
Last value in set.

Syntax:
  LAST_VALUE(c) OVER (...)

```sql
SELECT LAST_VALUE(s) OVER (ORDER BY s);
```

References:
- [Standard Library](../../../../../Docs/Reference/Standard_Library.md)
