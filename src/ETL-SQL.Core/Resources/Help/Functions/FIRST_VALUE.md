# FIRST_VALUE
First value in set.

Syntax:
  FIRST_VALUE(c) OVER (...)

```sql
SELECT FIRST_VALUE(s) OVER (ORDER BY s);
```

References:
- [Standard Library](../../../../../Docs/Reference/Standard_Library.md)
