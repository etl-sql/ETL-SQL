CREATE CONNECTION m ON MOCKDB();
CREATE CONNECTION pq ON PARQUET() WITH(PATH='samples/output/products_snapshot.parquet');

-- Stage data and write to Parquet
SELECT * INTO pq.Products FROM m.Products;

PRINT 'Parquet snapshot generated successfully.';
