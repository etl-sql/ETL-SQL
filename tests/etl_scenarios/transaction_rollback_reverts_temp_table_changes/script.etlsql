CREATE TABLE #TransactionAudit (ID INT, Label VARCHAR(20));
INSERT INTO #TransactionAudit (ID, Label) VALUES (1, 'before');

BEGIN TRANSACTION;
INSERT INTO #TransactionAudit (ID, Label) VALUES (2, 'during');
ROLLBACK TRANSACTION;
