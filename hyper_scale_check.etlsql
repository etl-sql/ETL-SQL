-- Verify Hyper-Scale Configuration Consumption

-- Set values
SET JOIN_SPILL_THRESHOLD = 50000;
SET WINDOW_SPILL_THRESHOLD = 25000;
SET EXTERNAL_HASH_PARTITIONS = 128;
SET EXTERNAL_SORT_CHUNK_SIZE = 4096;

-- Verification is usually via internal state, but we can use SHOW VARIABLES if implemented
-- Or just check if it fails or prints debug info.
PRINT 'Hyper-scale configurations applied successfully.';

-- Check if SHOW profiling shows them? (Depends on engine implementation)
-- For now, build success and parse success are the key blockers resolved.
