# ETL-SQL Testing Guide

The testing guide lives at [Docs/Testing.md](Docs/Testing.md).

Start there for:

- which lane to run for a change;
- what `fast`, `smoke`, `portal`, `integration`, `slt`, and pre-release validation cover;
- how to run SLT and scenario tests;
- current release-confidence status and remaining testing-foundation work.
